import * as React from 'react'

interface ResumePageProps {}

export default (props: ResumePageProps) =>
  <div>
    <h1>NOT FOUND</h1>
    <p>You just hit a route that doesn&#39;t exist... the sadness.</p>
  </div>
